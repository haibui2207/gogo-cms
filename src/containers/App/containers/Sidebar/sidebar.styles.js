import { createUseStyles } from 'react-jss';

export default createUseStyles({
  container: {
    height: '100%',
    zIndex: 1020,
    flex: 'none',
    display: 'inline-block',
    marginLeft: 0,
    position: 'relative',
  },
});
